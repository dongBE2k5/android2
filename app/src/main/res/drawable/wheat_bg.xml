<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#FFF5EA" />
    <corners android:radius="5dp" />
    <!-- Viền -->

    <!-- Bo góc (tuỳ chọn) -->
    <corners android:radius="10dp" />
</shape>
